import React from 'react';
import ReactDOM from 'react-dom/client';
import LaCoteRentals from './LaCoteRentals';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LaCoteRentals />
  </React.StrictMode>
);
